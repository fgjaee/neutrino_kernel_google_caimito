name: Kernel Build

on:
  push:
    branches:
      - "**"
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      # Pixel 9 Pro Fold is comet
      BUILD_CONFIG: google-devices/comet/build.config.comet
      CCACHE_DIR: /home/runner/.cache/ccache
      CCACHE_EXEC: /usr/bin/ccache

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Verify BUILD_CONFIG exists
        run: |
          echo "Using BUILD_CONFIG=$BUILD_CONFIG"
          test -f "$BUILD_CONFIG"
          echo "BUILD_CONFIG first lines:"
          sed -n '1,80p' "$BUILD_CONFIG"

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc \
            bison \
            build-essential \
            ccache \
            curl \
            flex \
            git \
            libelf-dev \
            libncurses5-dev \
            libssl-dev \
            python3 \
            rsync \
            unzip \
            zip \
            zlib1g-dev \
            lz4

      - name: Prepare ccache
        run: |
          mkdir -p "$CCACHE_DIR"
          ccache --set-config=cache_dir="$CCACHE_DIR"
          ccache --set-config=compression=true
          ccache --set-config=compression_level=6
          ccache --set-config=max_size=10G
          ccache -z || true

      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: /home/runner/.cache/ccache
          key: ${{ runner.os }}-ccache-${{ github.ref_name }}-${{ hashFiles('google-devices/comet/**', 'build.config.*', 'build/**') }}
          restore-keys: |
            ${{ runner.os }}-ccache-${{ github.ref_name }}-
            ${{ runner.os }}-ccache-

      - name: Sanity check key paths
        run: |
          echo "PWD=$(pwd)"
          ls -la
          ls -la google-devices/comet || true
          ls -la build || true
          ls -la "$BUILD_CONFIG" || true

      - name: Locate build scripts
        run: |
          echo "Listing repository root:"
          ls -la
          echo "Finding build scripts:"
          find . -maxdepth 4 -type f \( -name "build.sh" -o -name "*build*.sh" \) -print
          echo "Finding build config files:"
          find . -maxdepth 4 -type f -name "build.config.*" -print

      - name: Build Comet kernel
        run: |
          export CCACHE_DIR="$CCACHE_DIR"
          export CCACHE_EXEC="$CCACHE_EXEC"
          export USE_CCACHE=1
          mkdir -p out/dist
          ./build/build.sh --build_config="$BUILD_CONFIG" --dist_dir=out/dist

      - name: Locate kernel outputs
        if: always()
        run: |
          echo "Looking for kernel outputs under out/"
          find out -type f \( -name "Image.lz4" -o -name "Image" -o -name "dtbo.img" -o -name "boot.img" \) -print -exec ls -lh {} \; || true
          echo "Listing out/dist:"
          ls -la out/dist || true

      - name: ccache stats
        if: always()
        run: |
          ccache -s || true

      - name: Upload dist artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kernel-dist
          path: |
            out/dist/*
          if-no-files-found: error
